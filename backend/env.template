# Voldermot Diary Backend Environment Variables
# Copy this file to .env and update with your values
# Or rename this file to .env

# Server Configuration
NODE_ENV=production
PORT=3012
DOMAIN=voldermotDiary.thatinsaneguy.com

# SSL/HTTPS Configuration (Optional - only needed for HTTPS)
# Uncomment and set these if you want to use HTTPS directly (without nginx)
# SSL_CERT_PATH=/etc/letsencrypt/live/voldermotDiary.thatinsaneguy.com/fullchain.pem
# SSL_KEY_PATH=/etc/letsencrypt/live/voldermotDiary.thatinsaneguy.com/privkey.pem

# Database Configuration (if using SQLite - default)
# The database file will be created automatically at: ./data/voldermot_diary.db

# Notes:
# - PORT: The port the backend will run on (default: 3012)
# - DOMAIN: The domain name for the server (default: localhost)
# - NODE_ENV: Set to 'production' for production, 'development' for dev
# - SSL paths: Only needed if running HTTPS directly (usually nginx handles this)
